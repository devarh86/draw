package com.project.common.utils

import androidx.lifecycle.MutableLiveData

object Constants {

    var showProDialogClick8th = MutableLiveData(false)
    const val SELECTED_IMAGES = "SELECTED_IMAGES"

    var paddingVertical = 0.toPx()
    var paddingHorizontal = 0.toPx()
    var isProWithInterOn = false
}
package com.project.common.utils.enums

import androidx.annotation.Keep

@Keep
enum class FeatureMainMenuOptions(val title: String) {
    FEATURED("Featured"),
    HOME("Home"),
    TEMPLATE("Template"),
    SAVE("Save")
}